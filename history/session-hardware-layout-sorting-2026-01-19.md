# Session: 硬件配额 + 前端布局 + 排序优化

**日期**: 2026-01-19  
**主题**: 硬件/软件分离搜索、前端三段式布局、评分排序规则

---

## 完成的工作

### 1. 硬件/软件分离搜索模式 ✅

**目标**: 硬件产品占比从 18% 提升到 40%

**新增关键词系统** (`crawler/tools/auto_discover.py`):
- `KEYWORDS_SOFTWARE`: 软件 AI 关键词（各地区）
- `KEYWORDS_HARDWARE`: 硬件专用关键词
  - AI芯片、人形机器人、具身智能、边缘AI、AI推理芯片等

**命令行支持**:
```bash
# 只搜索硬件
python3 tools/auto_discover.py --region us --type hardware

# 只搜索软件
python3 tools/auto_discover.py --region cn --type software

# 混合模式 (40%硬件 + 60%软件) - 默认
python3 tools/auto_discover.py --region all --type mixed

# 查看关键词
python3 tools/auto_discover.py --list-keywords --region us
```

---

### 2. 前端布局重构 ✅

**新三段式布局**:

```
┌─────────────────────────────────────────┐
│  🦄 本周黑马 (4-5分)                      │
│  [全部] [🔧 硬件] [💻 软件]               │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐        │
│  │     │ │     │ │     │ │     │        │
│  └─────┘ └─────┘ └─────┘ └─────┘        │
├─────────────────────────────────────────┤
│  ⚡ 快速发现                              │
│  [全部] [硬件] [编程] [Agent] [图像]...   │
│  ┌─────────────────────────────┐        │
│  │      Swipe Cards            │        │
│  └─────────────────────────────┘        │
├─────────────────────────────────────────┤
│  📈 更多推荐                              │
│  [全部] [🦄 黑马(4-5分)] [⭐ 潜力股(2-3分)]│
│  排序: [热度▼] 类型: [全部▼]              │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐        │
│           [加载更多]                      │
└─────────────────────────────────────────┘
```

**修改的文件**:
- `frontend/views/index.ejs` - HTML 布局
- `frontend/public/css/style.css` - 筛选按钮样式
- `frontend/public/js/main.js` - 筛选逻辑、分页、排序

**删除的元素**:
- Hero 区域下方的分类标签（编程开发、语音识别等）

---

### 3. 新增 API 端点 ✅

| 端点 | 说明 |
|------|------|
| `/products/rising-stars` | **新增** 潜力股产品 (2-3分) |

**修改的文件**:
- `backend/app/routes/products.py` - 新增路由
- `backend/app/services/product_service.py` - 新增服务方法

---

### 4. 排序规则优化 ✅

**新排序逻辑** (评分 > 融资 > 估值/用户数):

| 优先级 | 条件 | 说明 |
|--------|------|------|
| 1️⃣ | **评分** | 5分 > 4分 > 3分 > 2分 |
| 2️⃣ | **融资金额** | 同分时，$500M > $100M > $50M |
| 3️⃣ | **估值/用户数** | 融资相同时，估值 > 用户数 > 热度分 |

**新增函数**:
- `_parse_funding()` - 解析融资金额字符串
- `_get_valuation_score()` - 计算估值/用户数分数
- `_sort_by_score_funding_valuation()` - 统一排序方法

---

### 5. 定时任务状态检查

**结果**: 定时任务**未安装**

```
launchctl list | grep weeklyai  → 未找到
~/Library/LaunchAgents/com.weeklyai.crawler.plist → 不存在
```

**安装命令**:
```bash
cp ops/scheduling/com.weeklyai.crawler.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.weeklyai.crawler.plist
```

---

## 关键文件变更

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `crawler/tools/auto_discover.py` | 修改 | 硬件/软件关键词分离，--type 参数 |
| `frontend/views/index.ejs` | 修改 | 三段式布局，删除分类标签 |
| `frontend/public/css/style.css` | 修改 | 筛选按钮、Tier Tabs 样式 |
| `frontend/public/js/main.js` | 修改 | 筛选逻辑、排序函数、分页 |
| `backend/app/routes/products.py` | 修改 | 新增 rising-stars 端点 |
| `backend/app/services/product_service.py` | 修改 | 排序函数、潜力股方法 |

---

## 当前数据状态

```
总产品: 48
硬件: 9 (18%) → 目标 40%
软件: 39 (81%)
```

**提升硬件占比**:
```bash
python3 tools/auto_discover.py --region all --type hardware
```

---

## 下一步建议

1. **安装定时任务** - 启用每日自动更新
2. **运行硬件搜索** - 提升硬件产品占比
3. **前端测试** - 验证新布局和筛选功能

---

*Session 结束: 2026-01-19*
