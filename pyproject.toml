[tool.ruff]
target-version = "py39"
line-length = 120
extend-exclude = [
  "crawler/data",
  "crawler/logs",
  "frontend",
  "history",
  "ops",
  "tests",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
